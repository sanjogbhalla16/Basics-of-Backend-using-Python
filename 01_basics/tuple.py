# major difference between dict and tuple is that dict is mutable so it can change
# but tuple is immutable so it does not change

tea_types = ("masala", "green", "lemon", "oolong")

print(tea_types)

print(tea_types[0])

# you cannot make this

# tea_types[0] = "bhaiji"
